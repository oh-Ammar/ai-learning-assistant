<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for reading and writing external storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIQuizGenerator"
        tools:targetApi="31">

        <!-- Activity for generating quizzes -->
        <activity
            android:name=".GenerateQuiz"
            android:exported="false" />

        <!-- Activity for document summarization -->
        <activity
            android:name=".DocumentSummarizer"
            android:exported="false" />

        <!-- Activity for converting documents to text -->
        <activity
            android:name=".DocumentToText"
            android:exported="false" />

        <!-- Activity for displaying output text -->
        <activity
            android:name=".OutputTextActivity"
            android:exported="false" />

        <!-- Main launcher activity -->
        <activity
            android:name=".FrontPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
